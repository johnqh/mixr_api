# Local Environment Overrides
# Copy this file to .env.local and customize with your local values
# .env.local is gitignored and will override values from .env

# Database Configuration
DATABASE_URL=postgresql://username:password@localhost:5432/mixr_db

# LM Studio Configuration (optional - using local LM Studio on port 1234)
# The API key can be any value when using LM Studio locally
OPENAI_API_KEY=lm-studio

# Server Configuration
PORT=6174
NODE_ENV=development
